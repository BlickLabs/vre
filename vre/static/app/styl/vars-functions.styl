// Colours
$orange = #EB944A
$lighterGray = #f5f5f5
$lightGray = #7F7E7E
$gray = #575656
$darkGray = #343434

// Media queries strings
$mediumWindow = "screen and (max-width: 1200px)"
$smallBase = "screen and (max-width: 768px)"
$smallLandscape = "screen and (max-width: 768px) and (orientation: landscape)"
$smallPortrait = "screen and (max-width: 768px) and (orientation: portrait)"
$hybridMobileTablet = "screen and (max-width: 773px) and (orientation: landscape), screen and (max-width: 767px) and (orientation: portrait)"
 
//Font sizes
$mainFontSize = 18px
$sectionTitleSize = 22px
$mainTitle = 28px

//Responsive font sizes

//Font weights
$fontLight = 300
$fontNormal = 400
$fontSemiBold = 600
$fontBold = 700

// Functions
simple-border(side, width, colour, style = solid)
  border-color colour
  border-style style
  if side == 'top'
    border-width width 0 0 0
  else if side == 'right'
    border-width 0 width 0 0
  else if side == 'bottom'
    border-width 0 0 width 0
  else if side == 'left'
    border-width 0 0 0 width
  else if side == 'topbottom'
    border-width width 0
  else if side == 'leftright'
    border-width 0 width
  else if side == 'notop'
    border-width 0 width width width
  else if side == 'noright'
    border-width width 0 width width
  else if side == 'nobottom'
    border-width width width 0 width
  else if side == 'notop'
    border-width width width width 0
  else if side == 'all'
    border-width width
  else
    border-width width
    
center-block()
  margin-left auto
  margin-right auto